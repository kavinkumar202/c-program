#include <stdio.h>
int main()
{
    int n,bill[10];
    printf("enter the size of the array:");
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&bill[i]);
        printf("values of the array: %d",bill[i]);
    }
    if(bill[i]<10)
    {
     for(int i=0;i<n;i++)
     {
printf("")
     }

    }
}
