#include<stdio.h>
#include<stdlib.h>
struct node
{
    int data;
    struct node * next;
}*head=NULL,*temp,*newnode,*prev;
void create()
{
    int n,i;
    printf("enter how many ele you have to create and insert:");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        newnode=struct *node
    }
}
